<UserControl x:Class="QueryPressure.WinUI.Views.ScriptView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QueryPressure.WinUI.Views"
             xmlns:viewModels="clr-namespace:QueryPressure.WinUI.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance d:Type=viewModels:ScriptViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="HELLO WORLD!"/>
        <TextBox Grid.Row="1" Text="{Binding Script}" AcceptsReturn="True" />
    </Grid>
</UserControl>
