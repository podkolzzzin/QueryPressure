<UserControl x:Class="QueryPressure.WinUI.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QueryPressure.WinUI.Views"
             xmlns:extensions="clr-namespace:QueryPressure.WinUI.Common.Extensions"
             xmlns:commands="clr-namespace:QueryPressure.WinUI.Commands"
             xmlns:system="clr-namespace:System;assembly=System.Runtime"
             xmlns:converters="clr-namespace:QueryPressure.WinUI.Common.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:EqualsConverter x:Key="EqualsConverter" />
        <converters:EnumDescriptionConverter x:Key="EnumDescriptionConverter" />
    </UserControl.Resources>
    <Menu>
        <MenuItem Header="{extensions:Locale labels.menu.file.title}">
            <MenuItem Header="{extensions:Locale labels.menu.file.create-new}" 
                      Command="{extensions:CommandImport commands:CreateNewProjectCommand}"/>
            <MenuItem Header="{extensions:Locale labels.Rename}" 
                      Command="{extensions:CommandImport commands:RenameProjectCommand}"
                      CommandParameter="WORLD!"/>
            <MenuItem Header="{extensions:Locale labels.menu.file.exit}"/>
        </MenuItem>
        <MenuItem Header="{extensions:Locale labels.menu.view.title}">
            <MenuItem Header="{extensions:Locale labels.menu.view.language}">
                <ItemsControl ItemsSource="{Binding Languages, Mode=OneTime}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <MenuItem Header="{extensions:Locale Binding = {Binding StringFormat='languages.{0}'}}"
                                          Command="{extensions:CommandImport commands:SetLanguageCommand}"
                                          CommandParameter="{Binding }">
                                <MenuItem.IsChecked>
                                    <MultiBinding Converter="{StaticResource EqualsConverter}" Mode="OneWay">
                                        <Binding Mode="OneTime"/>
                                        <extensions:Locale/>
                                    </MultiBinding>
                                </MenuItem.IsChecked>
                            </MenuItem>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </MenuItem>
            <MenuItem Header="{extensions:Locale labels.menu.view.theme}">
                <ItemsControl ItemsSource="{Binding Themes, Mode=OneTime}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <MenuItem Header="{extensions:Locale Binding = {Binding Converter={StaticResource EnumDescriptionConverter}}}"
                                          Command="{extensions:CommandImport commands:SetThemeCommand}"
                                          CommandParameter="{Binding }">
                                <MenuItem.IsChecked>
                                    <MultiBinding Converter="{StaticResource EqualsConverter}" Mode="OneWay">
                                        <Binding Mode="OneTime"/>
                                        <extensions:Theme/>
                                    </MultiBinding>
                                </MenuItem.IsChecked>
                            </MenuItem>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </MenuItem>
        </MenuItem>
        <MenuItem Header="{extensions:Locale labels.menu.about.title}">
            <MenuItem Header="{extensions:Locale labels.menu.about.version}"/>
        </MenuItem>
    </Menu>
</UserControl>
