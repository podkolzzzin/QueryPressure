<UserControl x:Class="QueryPressure.WinUI.Views.ExecutionResultsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:QueryPressure.WinUI.ViewModels"
             xmlns:local="clr-namespace:QueryPressure.WinUI.Views"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance d:Type=viewModels:ExecutionViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Text="{Binding Title}"/>
    </Grid>
</UserControl>
