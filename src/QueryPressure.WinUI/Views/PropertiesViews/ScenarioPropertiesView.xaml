<UserControl x:Class="QueryPressure.WinUI.Views.PropertiesViews.ScenarioPropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QueryPressure.WinUI.Views.PropertiesViews"
             xmlns:properties="clr-namespace:QueryPressure.WinUI.ViewModels.Properties"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance d:Type=properties:ScenarioPropertiesViewModel}"
             d:DesignHeight="450" d:DesignWidth="300"
             MinWidth="150" MinHeight="50">
    <Grid Margin="8" IsEnabled="{Binding CanEdit}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="70"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="24"/>
            <RowDefinition Height="Auto" MinHeight="24"/>
            <RowDefinition Height="Auto" MinHeight="24"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Content="Name:"/>
        <TextBox Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" Text="{Binding Name, UpdateSourceTrigger=LostFocus}"/>

        <Label Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Content="Provider:"/>
        <ComboBox Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" 
                  ItemsSource="{Binding Providers}"
                  SelectedItem="{Binding Provider}"/>

        <Label Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Content="Connection String:"/>
        <DockPanel Grid.Row="2" Grid.Column="1" LastChildFill="True" VerticalAlignment="Center">
            <Button DockPanel.Dock="Right" Padding="8 0" Margin="4 0 0 0" Content="Test"/>
            <TextBox DockPanel.Dock="Left" Text="{Binding ConnectionString, UpdateSourceTrigger=LostFocus}"/>
        </DockPanel>

    </Grid>
</UserControl>
